%div(class="top")
  %h3 Pivotal Projects

%div(class="wrap")
  %div(id="tables")
    %p This page has a list of all the Pivotal Project you have access to.

    %table(width="100%" border="0" cellspacing="0" cellpadding="0")
      %tbody
        %tr(class="table-header")
          %th(width="50%")
            %h5 Account
          %th(width="50%")
            %h5 Project Name

        - @projects.each do |project|
          %tr
            %td= project.account
            %td= link_to project.name, project_stories_path(project)

%br
