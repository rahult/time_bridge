%h1 Listing stories

%table
  %tr
    %th Project
    %th Name
    %th Story type
    %th Estimate
    %th Current state
    %th Requested by
    %th Owned by
    %th User
    %th
    %th
    %th

  - @stories.each do |story|
    %tr
      %td= story.project
      %td= story.name
      %td= story.story_type
      %td= story.estimate
      %td= story.current_state
      %td= story.requested_by
      %td= story.owned_by
      %td= story.user_id
      %td= link_to 'Show', project_story_path(:project_id => story.project_id, :id => story.id)

%br
